module Test.Data.Semigroup where

import Test.QuickCheck (TestGroup(..), TestResult, quickCheck)
import Prelude
import Data.Semigroup

--propMultiplication :: (Semigroup m)=>m->m->m->Bool
--propMultiplication x y z = (x <> y) <> z == x <> (y <> z)

